# 🎯 ОЦЕНКА: Блок 2.2 — Структуры данных

**Дата:** 2026-06-07  
**Статус:** В процессе

---

## ЗАДАНИЕ 1: List, Tuple, Dict

Какую структуру выбрать для каждого случая? Почему одной строкой?

```
1. Список студентов, которого я часто пополняю
   Структура: ? 
   Почему: ?

2. Координаты точки (x, y), которые не меняются
   Структура: ?
   Почему: ?

3. Название → цена товаров в магазине
   Структура: ?
   Почему: ?

4. Уникальные ID пользователей (быстрый поиск)
   Структура: ?
   Почему: ?
```

**Твой ответ:**
```
[пиши здесь]
```

---

## ЗАДАНИЕ 2: Операции с структурами

Что выведет?

```python
a = [1, 2, 3, 4, 5]
b = a[1:3]
c = {1, 2, 2, 3}
d = {"name": "Bob", "age": 30}

print(b)           # ?
print(len(c))      # ?
print(d["name"])   # ?
```

**Твой ответ:**
```
print(b) → ?
print(len(c)) → ?
print(d["name"]) → ?
```

---

## ЗАДАНИЕ 3: List Comprehension

Напиши, что выведет:

```python
numbers = [1, 2, 3, 4]
squared = [x**2 for x in numbers]
print(squared)  # ?

even_only = [x for x in numbers if x % 2 == 0]
print(even_only)  # ?
```

**Твой ответ:**
```
squared → ?
even_only → ?
```

---

## ЗАДАНИЕ 4: Вложенные структуры

```python
data = {
    "teams": {
        "A": ["Alice", "Bob"],
        "B": ["Charlie", "Diana"]
    }
}

print(data["teams"]["A"][0])  # ?
print(type(data["teams"]["A"]))  # ?
```

**Твой ответ:**
```
data["teams"]["A"][0] → ?
type(data["teams"]["A"]) → ?
```

---

## 📝 Критерии оценки

✅ **Успех:** все 4 задания верны → **переходим на Блок 2.3 (Функции)!**

⚠️ **Частичный:** 3 верны → повтор конкретной части

❌ **Не прошёл:** меньше 3 → повтор блока

---

**Заполни и дай сигнал "проверяй"** 👇
