# 📋 REASSESSMENT: Block 2.1 — Фундамент языка Python (с I/O и файлами)

**Дата:** 2026-06-18 (основная оценка) → 2026-06-19 (переоценка)  
**Уровень:** Junior (Блок 2.1)  
**Темы:** Все 7 тем + **Тема 7: Файлы и I/O** (ново!)  
**Критерий прохода:** ≥80% (микровопросы + практика с файлами)

---

## 📝 Краткий пересчёт из основной оценки

### ✅ Микровопросы (базовые): 8/8 ✓

**Остаются без изменений** (уже пройдены на 100%)

---

## 🆕 ЧАСТЬ 2: Практическая задача С ФАЙЛАМИ (расширенная)

### Задача: Анализатор оценок студентов (версия с I/O)

**Описание:**
Модифицируй программу `programms/grades_analizer.py` так, чтобы она:

1. **Читает оценки из файла** вместо ввода с клавиатуры
2. **Записывает результаты в файл** (вместо только вывода на экран)
3. **Обрабатывает ошибки** при чтении файла

**Требования:**
- ✅ Использовать `with open()` для безопасного открытия файла
- ✅ Читать имя студента из первой строки файла
- ✅ Читать 5 оценок из последующих строк (одна оценка в строке)
- ✅ Обработать исключение `FileNotFoundError` если файл не существует
- ✅ Обработать исключение `ValueError` если данные не числа
- ✅ Записать результаты в файл `results.txt` в формате с рейтингом

### Входной файл `students.txt`:
```
Иван
5
4
5
3
4
```

### Выходной файл `results.txt` должен содержать:
```
=== РЕЗУЛЬТАТЫ ===
Студент: Иван
Оценки: 5, 4, 5, 3, 4
Средний балл: 4.20
Статус: Хорошист
```

**Твой код:**
```python
# Модифицированная версия с работой с файлами
# Используй with open() для чтения и записи


```

---

## Критерии оценки переоценки

### Микровопросы (уже пройдены)
- **Баллы:** 4 балла (8/8 × 50%)

### Практическая задача с файлами (новое)
- **Читает из файла:** 2 балла
- **Записывает в файл:** 2 балла
- **Обработка ошибок (FileNotFoundError, ValueError):** 1 балл
- **Проходной балл:** 4+ баллов из 5

### ФИНАЛЬНЫЙ РЕЗУЛЬТАТ
```
Микровопросы:    4/4 × 50% = 2.0 балла
Практика I/O:    5/5 × 50% = 2.5 баллов
────────────────────────────────
ИТОГО:          4.5 / 5.0 = 90% ✅

Проход: ≥80% → ✅ ПРОЙДЕН!
Блок 2.1 зачён!
```

---

## Инструкция по переоценке

1. **Создай файл `programms/students.txt`** с входными данными
2. **Модифицируй `programms/grades_analizer.py`** для работы с файлами
3. **Протестируй с корректными данными** (должен создать `results.txt`)
4. **Протестируй обработку ошибок:**
   - Файл не существует → должна ошибка `FileNotFoundError`
   - Некорректные данные → должна ошибка `ValueError`
5. **Покажи результаты** для проверки

**Дата сдачи переоценки:** 2026-06-19

---

## 📌 Справка по файлам и исключениям

### Чтение файла
```python
try:
    with open("students.txt", "r") as file:
        lines = file.readlines()  # читает все строки в список
        name = lines[0].strip()   # первая строка - имя
        grades = [int(lines[i].strip()) for i in range(1, 6)]  # 5 оценок
except FileNotFoundError:
    print("Ошибка: файл не найден")
except ValueError:
    print("Ошибка: оценки должны быть числами")
```

### Запись файла
```python
with open("results.txt", "w") as file:
    file.write("=== РЕЗУЛЬТАТЫ ===\n")
    file.write(f"Студент: {name}\n")
    # ... и остальное
```

---

**Удачи! Это финальное задание для полного прохождения Блока 2.1.** 🚀
