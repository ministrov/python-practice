# -*- coding: utf-8 -*-
"""
Блок 2.1 — Тема 2а: Переменные в Python
═════════════════════════════════════════

Что такое переменные? Как их создавать, менять, именовать.
"""

# ═══════════════════════════════════════════════════════════════════════════
# ЧАСТЬ 1: ЧТО ТАКОЕ ПЕРЕМЕННАЯ
# ═══════════════════════════════════════════════════════════════════════════

print("=" * 70)
print("ЧАСТЬ 1: ЧТО ТАКОЕ ПЕРЕМЕННАЯ")
print("=" * 70)

print("""
Переменная — это контейнер, который хранит данные для использования в коде.
Она позволяет сохранить значение (число, строку и т.д.) и использовать его
многократно, вместо того чтобы писать значение снова и снова.

АНАЛОГИЯ:
  Переменная = коробка с этикеткой
  Значение = содержимое коробки

  x = 5   →  Коробка с названием 'x' содержит число 5
""")

# Примеры
age = 25
name = "Alice"
temperature = 36.6
is_student = True

print(f"age = {age}")
print(f"name = {name}")
print(f"temperature = {temperature}")
print(f"is_student = {is_student}\n")

# ═══════════════════════════════════════════════════════════════════════════
# ЧАСТЬ 2: ОБЪЯВЛЕНИЕ ПЕРЕМЕННЫХ
# ═══════════════════════════════════════════════════════════════════════════

print("=" * 70)
print("ЧАСТЬ 2: ОБЪЯВЛЕНИЕ ПЕРЕМЕННЫХ")
print("=" * 70)

print("""
В Python нет специального ключевого слова для объявления переменной,
как в некоторых других языках (C: int x; / Java: int x = 5;).

Объявление происходит АВТОМАТИЧЕСКИ при первом присвоении значения.
""")

# Просто присвоим значение — переменная создалась!
city = "Moscow"
print(f"city = {city}")

# Можно присвоить переменной результат выражения
result = 10 + 5
print(f"result = {result}")

# Можно присвоить одну переменную другой
x = 42
y = x
print(f"x = {x}, y = {y}\n")

# ═══════════════════════════════════════════════════════════════════════════
# ЧАСТЬ 3: ИЗМЕНЕНИЕ ЗНАЧЕНИЙ ПЕРЕМЕННЫХ
# ═══════════════════════════════════════════════════════════════════════════

print("=" * 70)
print("ЧАСТЬ 3: ИЗМЕНЕНИЕ ЗНАЧЕНИЙ ПЕРЕМЕННЫХ")
print("=" * 70)

print("""
Переменные называются "переменными" потому что их значения можно менять.
Просто присвой новое значение — старое исчезнет.
""")

count = 10
print(f"Исходно: count = {count}")

count = 20
print(f"После изменения: count = {count}")

count = count + 5  # Можно использовать текущее значение
print(f"После увеличения: count = {count}\n")

# ═══════════════════════════════════════════════════════════════════════════
# ЧАСТЬ 4: ДИНАМИЧЕСКОЕ ИЗМЕНЕНИЕ ТИПА ДАННЫХ
# ═══════════════════════════════════════════════════════════════════════════

print("=" * 70)
print("ЧАСТЬ 4: ДИНАМИЧЕСКОЕ ИЗМЕНЕНИЕ ТИПА")
print("=" * 70)

print("""
Особенность Python: одна переменная может менять ТИП данных.
В других языках это не возможно (или требует переинициализации).

Это называется "динамической типизацией".
""")

data = 5
print(f"data = {data}, тип: {type(data)}")

data = "hello"
print(f"data = {data}, тип: {type(data)}")

data = [1, 2, 3]
print(f"data = {data}, тип: {type(data)}")

data = 3.14
print(f"data = {data}, тип: {type(data)}\n")

# ═══════════════════════════════════════════════════════════════════════════
# ЧАСТЬ 5: ПРАВИЛА ИМЕНОВАНИЯ ПЕРЕМЕННЫХ
# ═══════════════════════════════════════════════════════════════════════════

print("=" * 70)
print("ЧАСТЬ 5: ПРАВИЛА ИМЕНОВАНИЯ ПЕРЕМЕННЫХ")
print("=" * 70)

print("""
✅ РАЗРЕШЕНО:
  • Буквы (a-z, A-Z)
  • Цифры (0-9) — но НЕ в начале
  • Подчёркивание (_)

❌ ЗАПРЕЩЕНО:
  • Начинаться с цифры
  • Содержать дефисы, пробелы, спецсимволы
  • Использовать зарезервированные слова (if, for, class и т.д.)

ПРИМЕРЫ:
""")

# Правильные имена
my_var = 10
age_2024 = 25
_private = "value"
MAX_SIZE = 100
student_name = "Bob"

print("✅ Правильные имена:")
print(f"  my_var = {my_var}")
print(f"  age_2024 = {age_2024}")
print(f"  _private = {_private}")
print(f"  MAX_SIZE = {MAX_SIZE}")
print(f"  student_name = {student_name}")

print("\n❌ ОШИБКИ (если раскомментировать, будут ошибки):")
print("""
  2num = 5        # Начинается с цифры
  my-var = 10     # Содержит дефис
  my var = 15     # Содержит пробел
  for = 20        # Зарезервированное слово
""")

# ═══════════════════════════════════════════════════════════════════════════
# ЧАСТЬ 6: СТИЛИ ИМЕНОВАНИЯ
# ═══════════════════════════════════════════════════════════════════════════

print("\n" + "=" * 70)
print("ЧАСТЬ 6: ПОПУЛЯРНЫЕ СТИЛИ ИМЕНОВАНИЯ")
print("=" * 70)

print("""
1. snake_case (РЕКОМЕНДУЕТСЯ в Python)
   Используй подчёркивание для разделения слов
   my_number, student_age, max_size

   ✅ СТАНДАРТ PYTHON (PEP 8)

2. camelCase (не рекомендуется, но встречается)
   myNumber, studentAge, maxSize
   ❌ Избегай в Python (это Java-стиль)

3. PascalCase (только для классов)
   MyClass, StudentInfo
   ✅ Используй ТОЛЬКО для классов

4. CAPS_LOCK (только для констант)
   MAX_SIZE = 100
   PI = 3.14
   ✅ Используй для значений, которые не меняются
""")

# Примеры
my_age = 25  # ✅ Правильно (snake_case)
myAge = 25   # ❌ Неправильно для Python
class Student:  # ✅ Правильно (PascalCase для класса)
    pass
MAX_AGE = 120  # ✅ Правильно (константа)

print("✅ my_age = 25 (snake_case, правильно)")
print("❌ myAge = 25 (camelCase, не рекомендуется)")
print("✅ class Student: (PascalCase для класса, правильно)")
print("✅ MAX_AGE = 120 (константа, правильно)\n")

# ═══════════════════════════════════════════════════════════════════════════
# ЧАСТЬ 7: КОНСТАНТЫ В PYTHON
# ═══════════════════════════════════════════════════════════════════════════

print("=" * 70)
print("ЧАСТЬ 7: КОНСТАНТЫ В PYTHON")
print("=" * 70)

print("""
КОНСТАНТА — это переменная, значение которой НЕ должно меняться
на протяжении работы программы.

⚠️ ВАЖНО: В Python нет МЕХАНИЗМА для защиты констант (в отличие от C++).
Это просто ДОГОВОРЁННОСТЬ между программистами.

ПРАВИЛА:
  1. Обозначай константы CAPS_LOCK (большие буквы с подчёркиванием)
  2. Присвой значение один раз в начале программы
  3. НЕ меняй значение потом

АНАЛОГИЯ:
  Обычная переменная (snake_case):  count = 10
  Константа (CAPS_LOCK):             MAX_SIZE = 100
                                     PI = 3.14159
""")

# Примеры констант
PI = 3.14159
GRAVITY = 9.8
MAX_ATTEMPTS = 3
APP_NAME = "MyApp"
DATABASE_URL = "localhost:5432"

print("Примеры констант:")
print(f"  PI = {PI}")
print(f"  GRAVITY = {GRAVITY}")
print(f"  MAX_ATTEMPTS = {MAX_ATTEMPTS}")
print(f"  APP_NAME = {APP_NAME}")
print(f"  DATABASE_URL = {DATABASE_URL}\n")

print("""
ИСПОЛЬЗОВАНИЕ КОНСТАНТ (например, для расчётов):
""")

# Расчёт площади круга: S = π * r²
radius = 5
area = PI * radius ** 2
print(f"Радиус круга: {radius}")
print(f"Площадь: {area:.2f}")

print("\n" + "=" * 70)
print("ЧЕМ ПОЛЕЗНЫ КОНСТАНТЫ?")
print("=" * 70)

print("""
1. ЧИТАЕМОСТЬ
   Вместо: distance = distance / 3.14159
   Напиши: distance = distance / PI

   (PI понятнее, чем просто число)

2. УДОБСТВО ИЗМЕНЕНИЙ
   Если нужно изменить максимум попыток с 3 на 5:
   - Обновляешь одну строку: MAX_ATTEMPTS = 5
   - Все места, где используется MAX_ATTEMPTS, автоматически обновятся

3. ИЗБЕГАНИЕ ОШИБОК
   Если забыл значение — посмотри в начало файла (все константы там)
   Не нужно искать "3" в коде, когда на самом деле имелось в виду 3.14159

ПРИМЕРЫ РЕАЛЬНЫХ КОНСТАНТ:
  - PORT = 8080 (порт сервера)
  - TIMEOUT = 30 (время ожидания)
  - MIN_PASSWORD_LENGTH = 8 (минимальная длина пароля)
  - API_KEY = "sk-..." (ключ API)
""")

# ═══════════════════════════════════════════════════════════════════════════
# ЧАСТЬ 8: КОММЕНТАРИИ
# ═══════════════════════════════════════════════════════════════════════════

print("\n" + "=" * 70)
print("ЧАСТЬ 8: КОММЕНТАРИИ")
print("=" * 70)

print("""
Комментарий — это текст, который игнорируется интерпретатором.
Используй комментарии, чтобы объяснить код.

Синтаксис:
  # Однострочный комментарий
  '''
  Многострочный комментарий
  (на самом деле это строка)
  '''
""")

# Это однострочный комментарий
age = 25  # Присвоим переменной значение

print(f"age = {age}")

# Многострочный комментарий
"""
Этот текст не выполняется.
Это комментарий.
Используй для больших объяснений.
"""

print("\nКомментарии видны в коде, но не выполняются.\n")

# ═══════════════════════════════════════════════════════════════════════════
# ЧАСТЬ 9: МНОЖЕСТВЕННОЕ ПРИСВОЕНИЕ
# ═══════════════════════════════════════════════════════════════════════════

print("=" * 70)
print("ЧАСТЬ 9: МНОЖЕСТВЕННОЕ ПРИСВОЕНИЕ")
print("=" * 70)

print("""
Python позволяет присвоить несколько переменных одновременно.
""")

# Присвоить несколько переменных одно значение
x = y = z = 10
print(f"x = y = z = 10")
print(f"  x = {x}, y = {y}, z = {z}")

# Присвоить разные значения
a, b, c = 1, 2, 3
print(f"\na, b, c = 1, 2, 3")
print(f"  a = {a}, b = {b}, c = {c}\n")

# ═══════════════════════════════════════════════════════════════════════════
# ИТОГОВАЯ ШПАРГАЛКА
# ═══════════════════════════════════════════════════════════════════════════

print("=" * 70)
print("✅ ИТОГОВАЯ ШПАРГАЛКА")
print("=" * 70)

print("""
1️⃣ ОБЪЯВЛЕНИЕ
   name = "Alice"  # просто присвой значение

2️⃣ ИЗМЕНЕНИЕ
   age = 25
   age = 26  # новое значение

3️⃣ ДИНАМИЧЕСКИЙ ТИП
   x = 5        # int
   x = "hi"     # str (можно менять тип)

4️⃣ ПРАВИЛА ИМЁН
   ✅ my_var, age_2024, _private, MAX_SIZE
   ❌ 2var, my-var, for, if

5️⃣ СТИЛЬ
   ✅ snake_case (переменные)
   ✅ PascalCase (классы)
   ✅ CAPS_LOCK (константы)

6️⃣ КОММЕНТАРИИ
   # однострочный
   '''многострочный'''

7️⃣ МНОЖЕСТВЕННОЕ ПРИСВОЕНИЕ
   x = y = 10
   a, b = 1, 2
""")
