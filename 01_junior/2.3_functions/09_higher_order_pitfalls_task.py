# -*- coding: utf-8 -*-
"""
Блок 2.3.4: Закрепление — ссылка на функцию vs вызов функции
════════════════════════════════════════════════════════════════════════
Повод: в мини-тесте по library.py (filter/map/sorted) три ответа из
шести были неточными именно в этом месте — путаница между "передать
функцию как объект" и "вызвать функцию и передать результат".

Правило для каждого задания:
  1. Сначала, НЕ запуская код, впиши свой прогноз в комментарий:
     упадёт ли ошибка, и если да — какая именно и почему.
  2. Только после этого запусти код и сравни с прогнозом.
  3. Если прогноз не совпал — не исправляй молча, объясни себе (и мне),
     почему ты ошибся.

Совет: 07_functions_as_objects_demo.py — теория, если нужно освежить.
"""

print("=" * 60)
print("ЗАДАНИЕ 1: функция-ссылка vs вызванная функция в map()")
print("=" * 60)
print("""
def triple(n: int) -> int:
    return n * 3

nums = [1, 2, 3]

1.1 ПРОГНОЗ (впиши в комментарий, не запуская):
    result_a = list(map(triple, nums))
    -> ???

1.2 ПРОГНОЗ:
    result_b = list(map(triple(nums[0]), nums))
    -> ???

Затем допиши обе строки кода и проверь себя.
""")

# ТВОЙ ПРОГНОЗ 1.1:
# [3, 6, 9]
# ТВОЙ ПРОГНОЗ 1.2:
# TypeError: 'int' object is not callable
# ТВОЙ КОД ЗДЕСЬ:


def triple(n: int) -> int:
    return n * 3


nums: list[int] = [1, 2, 3]
result_a: list[int] = list(map(triple, nums))
print("result_a =", result_a)

# type: ignore # упадёт с ошибкой
# result_b: list[int] = list(map(triple(nums[0]), nums))
# print("result_b =", result_b)

print("\n" + "=" * 60)
print("ЗАДАНИЕ 2: filter() с кортежами — распаковка внутри lambda")
print("=" * 60)
print("""
items = [(1, -5), (2, 3), (3, -1), (4, 8)]
# пары (id, баланс) — нужно оставить только с положительным балансом

2.1 ПРОГНОЗ:
    ok_a = list(filter(lambda pair: pair[1] > 0, items))
    -> ???

2.2 ПРОГНОЗ (обрати внимание на сигнатуру lambda):
    ok_b = list(filter(lambda item_id, balance: balance > 0, items))
    -> ???

Допиши обе строки, сверь с прогнозом. Если 2.2 упал — объясни
словами: сколько аргументов получает lambda на каждый вызов filter(),
и почему это не то же самое, что map(func, list1, list2).
""")

items: list[tuple[int, int]] = [(1, -5), (2, 3), (3, -1), (4, 8)]

# ТВОЙ ПРОГНОЗ 2.1:
# lambda получает pair целиком — один кортеж (id, balance).
# pair[1] > 0 проверяет баланс.
# -> [(2, 3), (4, 8)]
ok_a = list(filter(lambda pair: pair[1] > 0, items))
print("ok_a =", ok_a)
# ТВОЙ ПРОГНОЗ 2.2:
# filter() на каждой итерации передаёт lambda РОВНО ОДИН аргумент —
# сам элемент списка (весь кортеж целиком), а не распакованные id и balance.
# lambda с сигнатурой (item_id, balance) ждёт ДВА аргумента —
# получит один -> TypeError: missing 1 required positional argument
# ok_b = list(filter(lambda item_id, balance: balance > 0, items))
# print("ok_b =", ok_b)
# ТВОЁ ОБЪЯСНЕНИЕ (если 2.2 упал): filter() вызывает lambda один раз
# на каждый элемент items, передавая ОДИН аргумент — сам элемент
# (кортеж целиком). lambda(item_id, balance) ждёт два отдельных
# аргумента, а не один кортеж -> TypeError: missing 1 required
# positional argument: 'balance'. Это не то же самое, что
# map(func, list1, list2) — там на каждый шаг реально даются два
# ОТДЕЛЬНЫХ значения из двух разных итерируемых, а не один кортеж.

# ТВОЙ КОД ЗДЕСЬ:


print("\n" + "=" * 60)
print("ЗАДАНИЕ 3: sorted(key=...) — та же ловушка, другое место")
print("=" * 60)
print("""
words = ["kiwi", "fig", "banana", "date"]

3.1 ПРОГНОЗ:
    by_len_a = sorted(words, key=len)
    -> ???

3.2 ПРОГНОЗ (смотри внимательно на скобки):
    by_len_b = sorted(words, key=len(words[0]))
    -> ???

Допиши обе строки. Для 3.2 — если будет ошибка, укажи в комментарии
ТИП ошибки (например TypeError/NameError) до запуска, потом проверь,
угадал ли тип.
""")

# ТВОЙ ПРОГНОЗ 3.1:
# ТВОЙ ПРОГНОЗ 3.2 (тип ошибки):

# ТВОЙ КОД ЗДЕСЬ:


print("\n" + "=" * 60)
print("ЗАДАНИЕ 4: map() с ДВУМЯ итерируемыми — когда lambda с двумя "
      "параметрами уместна")
print("=" * 60)
print("""
Из мини-теста: ты пытался писать map(lambda n, s: ..., pairs), где
pairs — список кортежей (ОДИН итерируемый). Двухпараметрическая lambda
подходит для другого случая — когда в map() передают ДВА раздельных
списка.

ids = [1, 2, 3]
balances = [-5, 3, 8]

4.1 Напиши через map() список строк вида "id:balance" для каждой пары
    id/balance ИЗ ДВУХ РАЗНЫХ списков (не из списка кортежей!).
    Подсказка: map(lambda a, b: ..., ids, balances) — тут двухпара-
    метрическая lambda работает, потому что map получает по одному
    элементу из КАЖДОГО списка одновременно.

4.2 Одним предложением объясни себе: чем "map(func, list1, list2)"
    отличается от "map(func, list_of_pairs)" с точки зрения того,
    сколько аргументов получает func на каждый вызов.
""")

# ТВОЁ ОБЪЯСНЕНИЕ 4.2:

# ТВОЙ КОД ЗДЕСЬ:


print("\n" + "=" * 60)
print("ЗАДАНИЕ 5: недостающий аргумент — где именно упадёт ошибка")
print("=" * 60)
print("""
def add_tax(price: float, rate: float) -> float:
    return price * (1 + rate)

prices = [100, 200, 300]

5.1 ПРОГНОЗ: где именно (на какой строке/этапе) и с каким сообщением
    упадёт код ниже — ДО того как map начнёт перебирать prices, или
    ВНУТРИ перебора, на первом элементе?
    result = list(map(add_tax(0.2), prices))

5.2 Допиши строку из 5.1, проверь прогноз.

5.3 Напиши рабочий вариант через lambda, который применяет add_tax
    с фиксированным rate=0.2 к каждой цене из prices.
""")

# ТВОЙ ПРОГНОЗ 5.1:

# ТВОЙ КОД ЗДЕСЬ:


print("\n" + "=" * 60)
print("КОНЕЦ. Правило на будущее: каждый прогноз — ДО запуска, "
      "не после.")
print("=" * 60)
